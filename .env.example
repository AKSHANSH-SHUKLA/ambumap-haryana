# Copy to backend/.env (and frontend/.env if needed)

# Backend
DATABASE_URL=postgresql+psycopg2://ambumap:ambumap@localhost:5432/ambumap
OSRM_HOST=http://router.project-osrm.org
FLASK_PORT=5001
FLASK_ENV=development
CORS_ORIGINS=http://localhost:5173

# Frontend (Vite reads VITE_* vars)
VITE_API_BASE=http://localhost:5001
